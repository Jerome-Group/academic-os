# MODULE_CODE — Glossary

The module's own terms: its notation, the names it gives things, and the conventions it keeps that
another module writes differently.

What belongs here rather than in `CONTEXT.md`, and when a term is worth minting at all, is in
`docs/20 Teaching Procedure.md`.
