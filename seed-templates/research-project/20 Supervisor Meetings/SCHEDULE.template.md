# Supervisor meeting workspaces

The Calendar owns confirmed meeting dates. Add a row when its durable meeting folder is created.
`Meeting.md` owns the local lifecycle state.

| Date | Folder | State |
| --- | --- | --- |
